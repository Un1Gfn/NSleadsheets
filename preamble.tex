\usepackage[
  a4paper,
  portrait,
  hmargin=15mm,
  vmargin=15mm,
  % showframe,
]{geometry}

% \usepackage{fancyhdr}

\usepackage[stamp=true,text={D R A F T},scale=1.01,colorspec=.95]{draftwatermark}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% \usepackage{multicol}

% \usepackage{parskip}
\setlength{\parindent}{0pt}

% \usepackage[chordbk]{songbook}
% \usepackage[chorded]{songs}
\usepackage[full]{leadsheets}
\useleadsheetslibrary{musejazz}

% \usepackage{listofitems}

% \usepackage{pgffor}

\newenvironment{refrain}[0]{
  \begin{verse}[label-format={Refrain}]
}{
  \end{verse}
}

\newenvironment{together}[0]{
  \begin{verse}[label-format={Together}]
}{
  \end{verse}
}

% Outro is defined but undocumented
\newenvironment{outroo}[0]{
  \begin{verse}[label-format={Outro}]
}{
  \end{verse}
}

\newenvironment{acappella}[0]{
  \begin{verse}[label-format={a cappella}]
    (voices only / without instruments) \\
}{
  \end{verse}
}

\usepackage{nameref}

\usepackage{color}
\usepackage{hyperref}
\newcommand{\backtotoc}[0]{\hyperref[lbtoc]{back to TOC}}
\hypersetup{
    colorlinks=true,
    linktoc=all,
    % citecolor=black,
    % filecolor=black,
    linkcolor=blue,
    % urlcolor=black
}

\usepackage{datetime}

\usepackage{progressbar}
